<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".TimeActivity">

    <ToggleButton
        android:id="@+id/timeFeatureToggleButton"
        style="@style/locationFeatureToggleButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:onClick="onToggleClicked"
        android:textOff="@string/feature_switch_off"
        android:textOn="@string/feature_switch_on" />

    <ListView
        android:id="@+id/timeFeatureListView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@+id/timeFeatureToggleButton"
        android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginRight="@dimen/activity_horizontal_margin"
        android:layout_marginTop="4dp" />

    <Button
        android:id="@+id/add_button"
        android:layout_width="100dip"
        android:layout_height="100dip"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:width="100dip"
        android:height="100dip"
        android:background="@drawable/roundedbutton"
        android:onClick="addApplication"
        android:text="+" />

</RelativeLayout>
